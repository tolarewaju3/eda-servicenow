---
- name: Listen for events from service now
  sources:
   - ansible.eda.webhook:
      host: 0.0.0.0
  rules:
   - name: Test rule
     action:
       debug:
        msg:
          - "{{ event }}"
