---
- name: Listen for events from service now
  sources:
   - ansible.eda.servicenow:
      host: 0.0.0.0
  rules:
   - name: Test rule
     condition: event != ''
     action:
       debug:
        msg:
          - "{{ event }}"
